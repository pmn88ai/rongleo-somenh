# This folder is the original default Next.js scaffold.
# It has been superseded by src/app/ which contains the actual app.
# Do NOT delete this file — it serves as a marker that this folder is intentionally inactive.
# Next.js reads src/app/ when both src/ and root app/ exist (src/ takes priority).
