// src/insights/baziExtractor.ts
// Stub extractor for Bazi (Tứ Trụ) engine
// Full implementation can be added when Bazi engine is ready

import type { BaziData } from "@/engines/bazi/types"
import type { Insight }  from "./types"

export function extractBaziInsights(_bazi: BaziData): Insight[] {
  // Stub — returns empty array until Bazi engine is implemented
  return []
}
